import React from 'react'

import {LoadingIcon} from 'app/assets'

export const LoadingNotification = React.createClass({
  render () {
    return (
      <LoadingIcon className='c-loading-notification' />
    )
  }
})
